from PIL import Image

# X_LEN = 1000
# Y_LEN = 1000

# img = Image.new( 'RGB', (X_LEN, Y_LEN), "white") # Create a new black image
# pixels = img.load() # Create the pixel map
# for i in range(X_LEN):    # For every pixel:
#     for j in range(Y_LEN):
#         if i in (100, 900) and 10 <= j <= 990 or 100 <= i <= 900 and j in (10, 990):
#             pixels[i,j] = (0, 0, 100) # Set the colour accordingly


img.show()